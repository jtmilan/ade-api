PORT=8787
HOST=0.0.0.0
# Stripe (never ship these to the Tauri desktop binary)
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
STRIPE_PRICE_PRO=price_xxx
STRIPE_PRICE_TEAM=price_xxx
# App URLs
PUBLIC_APP_URL=https://app.example.com
CHECKOUT_SUCCESS_URL=https://app.example.com/billing/success
CHECKOUT_CANCEL_URL=https://app.example.com/billing/cancel
# Entitlements signing (Ed25519 seed base64 or HMAC fallback secret)
ENTITLEMENTS_SIGNING_SECRET=change-me-to-a-long-random-string
# Dev only — allow unsigned local entitlements
ALLOW_DEV_AUTH=true
